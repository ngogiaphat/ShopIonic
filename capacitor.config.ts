import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.shopionic',
  appName: 'shopionic',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
